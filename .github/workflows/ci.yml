name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - '.devcontainer/**'
      - '.vscode/**'
      - '**.md'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '.devcontainer/**'
      - '.vscode/**'
      - '**.md'
  workflow_dispatch:

permissions: read-all

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # NOTE: Need to status checks to be shown in PR
          # https://www.brianperry.dev/til/2021/configuring-lighthouse-ci-with-github-actions/
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Set up PNPM
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install PNPM deps
        run: pnpm install

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps

      - name: Check code formatting
        run: pnpm exec prettier --check .

      - name: Lint code
        run: pnpm exec eslint .

      - name: Run type checks
        run: pnpm exec tsc --noEmit

      - name: Run tests
        run: pnpm run test

      - name: Upload coverage report
        uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          files: coverage/clover.xml
          flags: unit

      - name: Run end-to-end tests
        run: pnpm run e2e

      - name: Upload Playwright reports
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: playwright-artifacts
          path: |
            playwright-report/
            test-results/
          retention-days: 7

      - name: Build site
        run: pnpm run build

      # TODO: For testing purpose; REMOVE it before PR merge
      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v10
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
        with:
          urls: https://lasuillard.github.io/
          budgetPath: ./budget.json
          temporaryPublicStorage: true

      - name: Upload build artifact
        if: ${{ github.event_name == 'push' }}
        uses: actions/upload-pages-artifact@v2
        with:
          path: build/

  deploy:
    name: Deploy
    needs: build
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url}}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v10
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
        with:
          urls: ${{ steps.deployment.outputs.page_url }}
          uploadArtifacts: true
