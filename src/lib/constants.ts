/**
 * Number of blog posts to show per page.
 */
export const PAGE_SIZE = 5;

/**
 * Maximum number of pages visible in the pagination bar before collapsing with ellipsis.
 */
export const MAX_VISIBLE_PAGES = 7;

/**
 * The page number threshold below which ellipsis is shown near the end of the list.
 */
export const START_ELLIPSIS_THRESHOLD = 4;

/**
 * The offset from the total page count below which ellipsis is shown near the start of the list.
 */
export const END_ELLIPSIS_OFFSET = 3;

/**
 * Maximum pages shown at the start/end sequence when ellipsis is collapsed on one side.
 */
export const NEAR_START_END_LIMIT = 5;
